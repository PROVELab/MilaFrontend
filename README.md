## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ### Backend

   * `cd` into `backend` package and run the server by running 
   
   ```bash
   node server.js
   ```

   ### Frontend

   * run 
   
   ```bash
   npx expo start
   ```

